// import useUser from '../providers/UserProvider/UserProvider.hook';

function Profile() {
  // const { isLoggedIn } = useUser();

  // return <>{!isLoggedIn ? <AuthContainer /> : <p>welcome</p>}</>;
  return <></>;
}

export default Profile;
